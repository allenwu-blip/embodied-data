# AgiBot schema (stub — see `docs/schema/`)

The schema reference has been split per AgiBot variant as of v0.2:

- [`docs/schema/overview.md`](./schema/overview.md) — variant detection rules,
  empirical Alpha ≡ Beta finding, coverage matrix.
- [`docs/schema/digitalworld.md`](./schema/digitalworld.md) — sim variant
  (`agibot-world/AgiBotDigitalWorld`).
- [`docs/schema/beta.md`](./schema/beta.md) — real-hardware variant
  (`agibot-world/AgiBotWorld-Beta`, also applies to
  `agibot-world/AgiBotWorld-Alpha` per empirical schema equivalence).

Pre-v0.2 versions of `embodied-data` referenced this file directly; the
stub keeps those external links alive.
